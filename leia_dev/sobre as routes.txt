As routes são as rotas da aplicação, o React router possui esse diretório para arquivos específicos que renderizam e aplicam lógica no 
front end. 



Como desenvolvedora, mantenho as chamadas de API aqui nas rotas para evitar confusão e manter o código limpo de diretórios inutilizados.
Então o modelo de arquitetura continua sendo monolítica.

Atenção: NÃO exclua esse diretório ou aloque-o para outro lugar, este diretório faz parte do projeto raiz e há risco de quebrar o projeto.